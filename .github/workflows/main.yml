name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build Vanilla image
      run: make build
    - name: Push Vanilla image
      run: make push
    - name: Build Forge image
      run: make build_forge
    - name: Push Forge image
      run: make push_forge